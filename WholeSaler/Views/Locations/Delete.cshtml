@using Microsoft.AspNetCore.Localization
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@model WholeSaler.Models.Location

@using WholeSaler.Resources
@inject IHtmlLocalizer<SharedResource> SharedLocalizer


<form asp-action="Delete">
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <div class="d-flex justify-content-between">
                <a class="btn btn-secondary btn-icon-split" asp-action="Index">
                    <span class="icon text-white-50">
                        <i class="fas fa-arrow-left"></i>
                    </span>
                    <span class="text">@SharedLocalizer["Back to List"]</span>
                </a>
                <h6 class="m-0 font-weight-bold text-primary align-self-center">
                    @Localizer["Are you sure you want to delete this?"]
                </h6>
                <div>
                    <input type="submit" value="@SharedLocalizer["Delete"]" class="btn btn-danger" />
                </div>
            </div>
        </div>
        <div class="card-body">
            <center>
                <h4>@SharedLocalizer["Location"]</h4>
                <hr />
                <dl class="row">
                    <dt class="col-sm-6">
                        @Html.DisplayNameFor(model => model.Adress)
                    </dt>
                    <dd class="col-sm-6">
                        @Html.DisplayFor(model => model.Adress)
                    </dd>
                    <dt class="col-sm-6">
                        @Html.DisplayNameFor(model => model.LocationOwner)
                    </dt>
                    <dd class="col-sm-6">
                        @Html.DisplayFor(model => model.LocationOwner.Id)
                    </dd>
                    <dt class="col-sm-6">
                        @Html.DisplayNameFor(model => model.City)
                    </dt>
                    <dd class="col-sm-6">
                        @Html.DisplayFor(model => model.City.CityName)
                    </dd>
                </dl>
                <input type="hidden" asp-for="LocationID" />
            </center>
        </div>
    </div>
</form>